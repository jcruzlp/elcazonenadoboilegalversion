{% extends 'base.html.twig' %}

{% block title %}Skill index{% endblock %}

{% block body %}
    <h1>Skill index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Junior</th>
                <th>Senior</th>
                <th>Expert</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for skill in skills %}
            <tr>
                <td>{{ skill.id }}</td>
                <td>{{ skill.name }}</td>
                <td>{{ skill.junior }}</td>
                <td>{{ skill.senior }}</td>
                <td>{{ skill.expert }}</td>
                <td>
                    <a href="{{ path('app_crud_skill_show', {'id': skill.id}) }}">show</a>
                    <a href="{{ path('app_crud_skill_edit', {'id': skill.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_crud_skill_new') }}">Create new</a>
{% endblock %}
